<h1><%= @article.title %><h1>

<%= link_to 'Edit', edit_article_path(@article) %>
<%= link_to 'Delete Article', article_path(@article), method: :delete, data: { confirm: 'Are you sure want to delete this article?'} %>
